'use client';

import styles from './MetricsView.module.css';

/**
 * Metrics section view. Placeholder for future metrics content.
 * This is not a category for entries—it's a dedicated area for metrics/analytics.
 */
export default function MetricsView() {
  return (
    <div className={styles.metricsArea}>
      <span className={styles.metricsPlaceholder}>metrics area</span>
    </div>
  );
}
